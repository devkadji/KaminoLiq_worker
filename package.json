{
  "name": "kamino-liquidity-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: reliable cron + Telegram button for Kamino multiply liquidity",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  }
}
